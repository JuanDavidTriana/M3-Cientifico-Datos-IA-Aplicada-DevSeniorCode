# Copia este archivo a ".env" y referencia las variables que necesites
# desde docker-compose.yml con "env_file: .env" en cada servicio.
# El ".env" real NUNCA se sube al repositorio (ver ../README.md, sección 9.2).

LOG_LEVEL=INFO
